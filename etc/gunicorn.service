[Unit]
Description=gunicorn daemon
After=network.target

[Service]
User=sammy
Group=www-data
WorkingDirectory=/code/wordknow/application
ExecStart=gunicorn --access-logfile - --workers 3 --bind unix:/code/wordknow/application/app.sock project.wsgi:application

[Install]
WantedBy=multi-user.target
