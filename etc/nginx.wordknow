server {
    listen 80;
    server_name 0.0.0.0;

    location /static/ {
        root /app_static_files;
    }

    location / {
        include proxy_params;
        proxy_pass http://unix:/code/wordknow/application/app.sock;
    }
}
